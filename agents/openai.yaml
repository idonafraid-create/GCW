interface:
  display_name: "GCW"
  short_description: "Staged evidence-driven website reconstruction"
  default_prompt: "Use $gcw to establish a SITE_SPEC, build a scoped faithful baseline, and stop at the review gate before creative changes."
